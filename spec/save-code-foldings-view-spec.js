'use babel';

import SaveCodeFoldingsView from '../lib/save-code-foldings-view';

describe('SaveCodeFoldingsView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
